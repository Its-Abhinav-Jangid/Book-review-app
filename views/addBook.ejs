<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add New Book</title>

    <link rel="stylesheet" href="css/addBook.css" />
  </head>
  <body>
    <div id="modal" class="modal">
      <div class="modal-content">
        <span class="close-btn" onclick="closeModal()">&times;</span>
        <h2 id="modal-title">Book Title</h2>
        <!-- Added id for dynamic title -->

        <div class="book-details-modal">
          <p>
            <strong>Author:</strong> <span id="modal-author">John Doe</span>
          </p>
          <!-- Added id for dynamic author -->
          <p>
            <strong>Genre:</strong>
            <span id="modal-genre">Mystery, Thriller</span>
          </p>
          <!-- Added id for dynamic genre -->
          <p>
            <strong>Published:</strong> <span id="modal-published">2022</span>
          </p>
          <!-- Added id for dynamic published date -->
          <p><strong>Pages:</strong> <span id="modal-pages">320</span></p>
          <!-- Added id for dynamic page count -->
          <p>
            <strong>Publisher:</strong>
            <span id="modal-publisher">Penguin Books</span>
          </p>
          <!-- Added id for dynamic publisher -->
        </div>

        <div class="book-summary">
          <h3>Summary</h3>
          <p id="modal-summary">This book dives into a thrilling story...</p>
          <!-- Added id for dynamic summary -->
        </div>

        <div class="rating">
          <h3>Rating</h3>
          <span id="modal-rating">★★★★☆</span>
          <!-- Added id for dynamic rating -->
        </div>
        <div class="more-info">
          <a id="modal-link" href="#" target="_blank" style="color: #4db8ff"
            >More Info on Google Books</a
          >
          <!-- Added link for more info -->
        </div>
      </div>
    </div>

    <div class="add-book-container">
      <h2>Add a New Book</h2>
      <p class="instructions">
        Search for a book by title and click "Add" to include it in your list.
      </p>
      <form action="/add-book" method="get">
        <div class="search-bar">
          <input
            type="text"
            id="bookTitle"
            name="q"
            placeholder="Enter book name..."
            autocomplete="off"
          />
          <button id="searchButton" type="submit">Search</button>
        </div>
      </form>

      <div id="results" class="results-container">
        <% if (books.length) { %> <% books.forEach(book => { %>
        <div class="book-card">
          <img
            class="book-cover"
            <img
            class="book-cover"
            src="<%= book.volumeInfo.imageLinks ? book.volumeInfo.imageLinks.thumbnail : '/images/default-cover.jpg' %>"
          />

          <div class="book-details">
            <h3 class="book-title"><%= book.volumeInfo.title %></h3>
            <p class="book-author">
              <%= book.volumeInfo.authors ? book.volumeInfo.authors.join(', ') :
              'Unknown Author' %>
            </p>
            <p class="book-description">
              <%=book.volumeInfo.description ? book.volumeInfo.description :
              ''%>
            </p>

            <button
              class="add-button btn"
              onclick="addBook('<%= book.volumeInfo.title %>')"
            >
              Add
            </button>
            <button class="btn view-btn" onclick="openModal('<%=book.id%>')">
              View
            </button>
          </div>
        </div>
        <% }) %> <% } else { %>
        <p></p>
        <% } %>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="./js/addBook.js"></script>
  </body>
</html>
