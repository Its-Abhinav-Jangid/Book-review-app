<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add New Book</title>

    <link rel="stylesheet" href="css/addBook.css" />
  </head>
  <body>
    <!-- Updated Modal Structure -->
<div id="modal" class="modal">
  <div class="modal-content">
    <span class="close-btn" onclick="closeModal()">&times;</span>
    <h2>Book Title</h2>
    
    <div class="book-details-modal">
      <p><strong>Author:</strong> John Doe</p>
      <p><strong>Genre:</strong> Mystery, Thriller</p>
      <p><strong>Published:</strong> 2022</p>
      <p><strong>Pages:</strong> 320</p>
      <p><strong>Publisher:</strong> Penguin Books</p>
    </div>
    
    <div class="book-summary">
      <h3>Summary</h3>
      <p>This book dives into a thrilling story...</p>
    </div>
    
 

    <div class="rating">
      <h3>Rating</h3>
      <span>★★★★☆</span> <!-- Replace this with dynamic rating input -->
    </div>
  </div>
</div>

    <div class="add-book-container">
      <h2>Add a New Book</h2>
      <p class="instructions">
        Search for a book by title and click "Add" to include it in your list.
      </p>
      <form action="/add-book" method="get">
        <div class="search-bar">
          <input
            type="text"
            id="bookTitle"
            name="q"
            placeholder="Enter book name..."
            autocomplete="off"
          />
          <button id="searchButton" type="submit">Search</button>
        </div>
      </form>

      <div id="results" class="results-container">
        <% if (books.length) { %> <% books.forEach(book => { %>
        <div class="book-card">
          <img
            class="book-cover"
            <img
            class="book-cover"
            src="<%= book.volumeInfo.imageLinks ? book.volumeInfo.imageLinks.thumbnail : '/images/default-cover.jpg' %>"
          />

          <div class="book-details">
            <h3 class="book-title"><%= book.volumeInfo.title %></h3>
            <p class="book-author">
              <%= book.volumeInfo.authors ? book.volumeInfo.authors.join(', ') :
              'Unknown Author' %>
            </p>
            <p class="book-description">
              <%=book.volumeInfo.description ? book.volumeInfo.description :
              ''%>
            </p>

            <button
              class="add-button btn"
              onclick="addBook('<%= book.volumeInfo.title %>')"
            >
              Add
            </button>
            <button class="btn view-btn" onclick="openModal()">View</button>
          </div>
        </div>
        <% }) %> <% } else { %>
        <p></p>
        <% } %>
      </div>
    </div>

    <script >// JavaScript for Opening and Closing Modal
      function openModal() {
        document.getElementById("modal").style.display = "block";
      }
      
      function closeModal() {
        document.getElementById("modal").style.display = "none";
      }
      
      // Close modal if clicked outside of content
      window.onclick = function(event) {
        if (event.target == document.getElementById("modal")) {
          closeModal();
        }
      };
      </script>
  </body>
</html>
